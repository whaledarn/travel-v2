import * as React from "react";
import ErrorPage from "../components/ErrorPage";

const Error = () => {
  return (
    <React.Fragment>
      <ErrorPage></ErrorPage>
    </React.Fragment>
  );
};

export default Error;
