import * as React from "react";
import DNE from "../components/DNE";

const Nonexistent = () => {
  return (
    <React.Fragment>
      <DNE></DNE>
    </React.Fragment>
  );
};

export default Nonexistent;
